<%= form_with(model: @paciente, local: true) do |form| %>
  <div class="form-group">
    <%= form.label :nome, "Nome", class: "form-label" %>
    <%= form.text_field :nome, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :idade, "Idade", class: "form-label" %>
    <%= form.number_field :idade, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :endereco, "Endereço", class: "form-label" %>
    <%= form.text_field :endereco, class: "form-control" %>
  </div>

  <div class="form-group mt-3">
    <%= form.submit "Salvar", class: "btn btn-success" %>
  </div>
<% end %>
