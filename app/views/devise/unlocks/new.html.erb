<h2>Resend unlock instructions</h2>

<%= form_with(model: resource, as: resource_name, url: unlock_path(resource_name), local: true) do |form| %>
  <div class="form-group">
    <%= form.label :email, class: "form-label" %>
    <%= form.email_field :email, autofocus: true, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.submit "Resend unlock instructions", class: "btn btn-primary" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
