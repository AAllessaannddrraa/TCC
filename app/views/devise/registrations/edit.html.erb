<div class="container mt-4">
  <div class="card p-4">
    <h2 class="text-center">Editar Perfil</h2>

    <h2>Edit <%= resource_name.to_s.humanize %></h2>

    <%= form_with(model: resource, as: resource_name, url: registration_path(resource_name), local: true, html: { method: :put }) do |form| %>
      <div class="form-group">
        <%= form.label :email, class: "form-label" %>
        <%= form.email_field :email, autofocus: true, class: "form-control" %>
      </div>

      <div class="form-group">
        <%= form.label :password, class: "form-label" %>
        <%= form.password_field :password, autocomplete: "new-password", class: "form-control" %>
      </div>

      <div class="form-group">
        <%= form.label :password_confirmation, class: "form-label" %>
        <%= form.password_field :password_confirmation, autocomplete: "new-password", class: "form-control" %>
      </div>

      <div class="form-group">
        <%= form.submit "Update", class: "btn btn-primary" %>
      </div>
    <% end %>

    <hr class="my-4">

    <h3 class="text-danger">Cancelar minha conta</h3>
    <p>Se você não está satisfeito com nossos serviços, pode cancelar sua conta. Por favor, tome cuidado, pois essa ação é irreversível.</p>
    <div>
      <%= button_to "Cancelar minha conta", registration_path(resource_name), method: :delete, class: "btn btn-danger", data: { confirm: "Você tem certeza de que deseja cancelar sua conta?" } %>
    </div>
  </div>
</div>
